import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "비밀번호 재설정",
};

import NewPasswordSection from "@/components/page/newPassword";

export default function ResetPasswordPage() {}
